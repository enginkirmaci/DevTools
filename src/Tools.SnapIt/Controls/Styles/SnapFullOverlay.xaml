<Styles
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:control="using:Tools.SnapIt.Controls">

	<Style Selector="control|SnapFullOverlay">
		<Setter Property="VerticalAlignment" Value="Top" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="Focusable" Value="False" />
		<Setter Property="IsVisible" Value="False" />
		<Setter Property="Template">
			<ControlTemplate>
				<Grid
					x:Name="Overlay"
					Background="{Binding SnapTheme.HighlightBrush, RelativeSource={RelativeSource TemplatedParent}}"
					Opacity="0">
					<Border
						x:Name="OverlayBorder"
						BorderBrush="{Binding SnapTheme.BorderBrush, RelativeSource={RelativeSource TemplatedParent}}"
						BorderThickness="{Binding SnapTheme.BorderThickness, RelativeSource={RelativeSource TemplatedParent}}" />
				</Grid>
			</ControlTemplate>
		</Setter>
	</Style>
</Styles>
