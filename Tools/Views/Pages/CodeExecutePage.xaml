<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="Tools.Views.Pages.CodeExecutePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="Transparent"
	mc:Ignorable="d">

	<Grid Margin="0">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="9*" />
			<RowDefinition Height="5*" />
		</Grid.RowDefinitions>

		<Border
			Grid.Row="0"
			Margin="0,0,0,12"
			Padding="12"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="Transparent"
			BorderThickness="0"
			CornerRadius="8">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<TextBlock
					Grid.Column="1"
					VerticalAlignment="Center"
					Text="Write and execute C# code snippets instantly" />

				<Button
					Grid.Column="2"
					Height="36"
					Command="{x:Bind ViewModel.ExecuteCommand}"
					Style="{StaticResource AccentButtonStyle}">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<FontIcon FontSize="16" Glyph="&#xE768;" />
						<TextBlock Text="Execute Code" />
					</StackPanel>
				</Button>
			</Grid>
		</Border>

		<!--  Modern Code Input Card  -->
		<Border
			Grid.Row="1"
			Margin="0,0,0,12"
			Padding="12"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="Transparent"
			BorderThickness="0"
			CornerRadius="8">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<StackPanel Margin="0,0,0,16" Spacing="4">
					<TextBlock Text="Write and execute C# code snippets instantly" />
				</StackPanel>

				<TextBox
					x:Name="ImmediateInputTextBox"
					Grid.Row="1"
					MinHeight="200"
					AcceptsReturn="True"
					Background="Transparent"
					BorderThickness="0"
					FontFamily="Consolas"
					FontSize="13"
					PlaceholderText="Enter C# code to execute..."
					Text="{x:Bind ViewModel.ImmediateInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					TextWrapping="Wrap" />
			</Grid>
		</Border>

		<!--  Modern Output Card  -->
		<Border
			Grid.Row="2"
			Padding="12"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="Transparent"
			BorderThickness="0"
			CornerRadius="8">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<TextBlock
					Margin="0,0,0,12"
					Style="{StaticResource BodyStrongTextBlockStyle}"
					Text="Output" />

				<ScrollViewer Grid.Row="1">
					<TextBox
						FontFamily="Consolas"
						FontSize="12"
						IsReadOnly="True"
						Text="{x:Bind ViewModel.ImmediateOutput, Mode=OneWay}"
						TextWrapping="Wrap" />
				</ScrollViewer>
			</Grid>
		</Border>
	</Grid>
</Page>